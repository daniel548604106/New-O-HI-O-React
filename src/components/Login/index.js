import React from 'react';
import Login from './Login';
const index = () => {
  return (
    <div style={{ maxWidth: '80vw', width: '600px', display: 'block' }}>
      <Login />
    </div>
  );
};

export default index;
